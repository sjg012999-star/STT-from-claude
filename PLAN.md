# 학회·강연·회의 STT 시스템 — 전체 설계 계획

> Zoom H1e로 녹음한 학회 세미나(주 용도), 강연, 회의 음성을
> **여러 억양의 영어/한국어 → 자연스러운 전사 → 문맥 기반 교정 → 교정 내역 리포트 → 요약 정리**
> 까지 자동으로 처리하는 파이프라인.

---

## 1. 핵심 결론 요약

| 질문 | 결론 |
|---|---|
| 용도별(학회/강연/회의)로 STT 모델을 따로 만들어야 하나? | **아니오. 하나의 통합 파이프라인 + 용도별 "프로필"로 충분** |
| STT 모델을 직접 학습/파인튜닝해야 하나? | **아니오. Whisper large-v3 (또는 상용 API)로 충분. 억양 문제는 후처리로 해결** |
| 억양·전문용어 오인식은 어떻게 잡나? | **LLM(Claude) 후처리 계층**에서 앞뒤 문맥 + 도메인 용어집으로 교정 |
| 수정 내역은 어떻게 보여주나? | LLM이 구조화된 교정 목록(JSON)을 출력 + **diff 검증**으로 환각 방지 → 문서 끝에 표로 정리 |

### 왜 통합 모델 하나로 충분한가

학회/강연/회의의 차이는 **음향 모델(STT) 수준의 차이가 아니라 후처리 수준의 차이**입니다.

- **음향 측면**: 셋 다 "실내, 근거리~중거리 마이크, 1~수명의 화자, 자연 발화"라는 동일한 조건. Whisper large-v3는 68만 시간의 다국어·다억양 데이터로 학습되어 있어 인도식/중국식/한국식 영어 억양 등도 별도 학습 없이 처리 수준이 높습니다.
- **실제로 달라지는 것**:
  - 학회 세미나 → 전문용어 밀도가 높음, Q&A 구간 존재, 화자 수 적음
  - 강연 → 단일 화자, 구어체, 긴 흐름
  - 회의 → 다화자, 화자분리(diarization) 필요, 액션아이템 추출
- 이 차이는 전부 **(1) 도메인 용어집, (2) 화자분리 on/off, (3) 요약 템플릿**으로 흡수됩니다. 즉 STT 모델이 아니라 **설정 프로필**의 문제입니다.

파인튜닝은 "특정 화자/특정 분야에서 반복적으로 틀리는 패턴이 데이터로 쌓인 뒤"에나 고려할 마지막 수단이며, 그 전에 LLM 교정 계층이 같은 문제를 훨씬 싸고 유연하게 해결합니다. (교정된 전사 결과를 계속 저장해 두면, 훗날 파인튜닝이 필요해질 때 그대로 학습 데이터가 됩니다.)

---

## 2. 전체 아키텍처

```
Zoom H1e 녹음 (32-bit float WAV)
        │
        ▼
① 전처리 (ffmpeg / pydub)
   - 16kHz mono 다운샘플, 라우드니스 정규화(EBU R128)
   - VAD(무음 구간 제거) → 처리 시간·비용 절감
        │
        ▼
② STT (Whisper large-v3 / faster-whisper)
   - 언어 자동 감지 (ko/en, 코드스위칭 대응)
   - initial_prompt에 도메인 용어 주입
   - 세그먼트별 타임스탬프 + 신뢰도(avg_logprob) 확보
        │
        ▼
③ 화자분리 (pyannote.audio) ← 회의 프로필에서만 on
        │
        ▼
④ LLM 교정 계층 (Claude API)
   - 앞뒤 문맥 + 용어집 기반으로 오인식 단어 교정
   - 구조화된 교정 목록(JSON) 출력
   - difflib 검증으로 LLM 환각(원문 무단 변경) 차단
        │
        ▼
⑤ 정리·요약 (Claude API, 프로필별 템플릿)
   - 학회: 발표 요지 / 방법론 / 결과 / Q&A 정리
   - 강연: 목차형 요약 + 핵심 인용
   - 회의: 결정사항 / 액션아이템 / 담당자
        │
        ▼
⑥ 산출물
   - transcript.md  (교정된 전사 + 요약 + 교정 내역 표)
   - transcript.json (세그먼트/타임스탬프/교정 메타데이터)
   - transcript.srt  (자막 파일, 원본 대조용)
```

---

## 3. 단계별 상세 설계

### ① 전처리

Zoom H1e는 32-bit float, 44.1/48/96kHz로 녹음됩니다. Whisper 입력은 16kHz mono이므로 변환이 필수입니다.

```bash
ffmpeg -i input.wav -ac 1 -ar 16000 -af loudnorm=I=-16:TP=-1.5:LRA=11 prep.wav
```

- **32-bit float의 장점 활용**: 클리핑 걱정 없이 녹음 후 정규화 가능 → 강의실 뒷자리 녹음도 살릴 수 있음. 녹음 시 게인을 보수적으로 잡아도 됨.
- **VAD (Silero VAD)**: 1~2시간 세미나에서 무음/잡음 구간을 잘라내면 STT 시간과 LLM 토큰 비용이 크게 줄고, Whisper의 고질적 문제인 "무음 구간 환각(반복 문장 생성)"도 예방됩니다. faster-whisper에 `vad_filter=True`로 내장되어 있습니다.

### ② STT

**권장: faster-whisper + large-v3 (로컬)**

| 선택지 | 장점 | 단점 |
|---|---|---|
| **faster-whisper large-v3 (로컬)** ✅ | 무료, 프라이버시(미공개 연구 발표 녹음에 중요), initial_prompt 지원, 타임스탬프·신뢰도 확보 | GPU 필요 (RTX 3060 이상이면 1시간 음성 ≈ 5~10분) |
| OpenAI API (gpt-4o-transcribe 등) | 설치 불필요, 정확도 높음 | 25MB 파일 제한(분할 필요), 비용, 녹음 외부 전송 |
| 국내 API (클로바 스피치 등) | 한국어 최강 | 영어 혼용 발표에 약함, 비용 |

학회 발표는 **한 문장 안에서 한/영이 섞이는 코드스위칭**이 많습니다. Whisper는 세그먼트 단위로 언어를 처리하므로 완벽하지 않지만, 오인식분은 ④ LLM 교정에서 문맥으로 복원하는 전략을 씁니다.

핵심 기법 — **initial_prompt로 용어 주입**:

```python
from faster_whisper import WhisperModel

model = WhisperModel("large-v3", device="cuda", compute_type="float16")
segments, info = model.transcribe(
    "prep.wav",
    vad_filter=True,
    initial_prompt="본 발표는 CRISPR-Cas9, base editing, off-target effect에 관한 세미나입니다.",
    word_timestamps=True,
)
```

발표 자료(초록, 슬라이드 제목)에서 추출한 용어를 `initial_prompt`에 넣으면 전문용어 인식률이 크게 오릅니다. 학회 프로필에서는 "초록 텍스트 붙여넣기" 입력을 지원합니다.

### ③ 화자분리 (회의 프로필)

- **pyannote.audio 3.1** (HuggingFace 토큰 필요, 무료)
- Whisper 세그먼트 타임스탬프와 diarization 결과를 시간축으로 매칭 → `[화자A 00:12:34] ...` 형태로 출력
- 학회 프로필에서는 기본 off, Q&A 구간만 "발표자/질문자" 구분하는 경량 모드 옵션

### ④ LLM 교정 계층 — 이 시스템의 핵심

**목표**: 억양·소음으로 인한 오인식 단어를 앞뒤 문맥으로 복원하되, **원문을 마음대로 다시 쓰지 않게** 통제하고, 수정한 곳을 전부 추적.

**모델**: `claude-opus-4-8` (입력 $5 / 출력 $25 per 1M tokens)
- 전사 교정은 실시간이 아니므로 **Batch API 사용 시 50% 할인** → 사실상 $2.5/$12.5 수준
- 1시간 세미나 ≈ 전사 약 1만~1.5만 토큰 → 청크 분할·출력 포함해도 **회당 수백 원대**

**처리 방식**:

1. 전사를 **세그먼트 ID가 붙은 청크**(약 4천 토큰, 앞뒤 500토큰 오버랩)로 분할
2. 각 청크를 아래 계약으로 Claude에 전달:
   - 시스템 프롬프트(고정, **prompt caching** 적용): 교정 규칙 + 도메인 용어집
   - 입력: `[seg_012] 이 실험에서 크리스퍼 캐스나인을 사용해서...`
   - 출력(structured outputs, JSON schema 강제):

```json
{
  "corrected_segments": [
    {"id": "seg_012", "text": "이 실험에서 CRISPR-Cas9을 사용해서..."}
  ],
  "corrections": [
    {
      "segment_id": "seg_012",
      "original": "크리스퍼 캐스나인",
      "corrected": "CRISPR-Cas9",
      "reason": "음성 오인식 — 문맥상 유전자 편집 도구 명칭",
      "confidence": "high"
    }
  ]
}
```

3. **환각 방지 이중 검증**:
   - `difflib`로 원문 ↔ 교정문 실제 diff 계산
   - diff에는 있는데 `corrections` 목록에 없는 변경 = 무단 수정 → 원문으로 롤백하고 플래그
   - `confidence: low` 항목은 교정 반영하되 리포트에 ⚠️ 표시 (사람 확인 유도)
4. Whisper의 세그먼트 신뢰도(avg_logprob)가 낮은 구간을 프롬프트에 힌트로 전달 → "이 구간은 오인식 가능성이 높으니 중점 검토"

**교정 규칙 (시스템 프롬프트 핵심)**:
- 화자의 말투·어순·문체는 유지한다 (다듬기 금지, 오인식 복원만)
- 명백한 음성 오인식만 수정: 전문용어, 고유명사, 숫자/단위, 한영 혼용 표기
- 확신이 없으면 원문 유지 + `uncertain` 플래그
- 모든 수정은 corrections 배열에 기록 — 기록 없는 수정은 무효 처리됨을 명시

### ⑤ 정리·요약

교정 완료된 전체 전사를 입력으로 프로필별 템플릿 요약을 생성합니다. 전사가 길어도 Claude는 1M 컨텍스트라 1회 호출로 충분합니다.

- **학회 프로필**: 발표 개요 → 배경/문제의식 → 방법론 → 주요 결과 → 한계·향후 과제 → Q&A 정리(질문/답변 쌍)
- **강연 프로필**: 목차형 구조 요약 + 핵심 메시지 + 인상적 인용(타임스탬프 링크)
- **회의 프로필**: 안건별 논의 요약 → 결정사항 → 액션아이템(담당/기한)

### ⑥ 최종 산출물 (transcript.md 구조)

```markdown
# [제목] — 2026-07-06 세미나
## 요약
...
## 전사 (교정본)
[00:00:12] 안녕하세요, 오늘 발표할 주제는...
...
## 교정 내역 (총 23건)
| # | 시각 | 원문 | 수정 | 사유 | 확신도 |
|---|------|------|------|------|--------|
| 1 | 05:12 | 크리스퍼 캐스나인 | CRISPR-Cas9 | 용어 오인식 | 높음 |
| 2 | 12:40 | 피 밸류 | p-value | 통계 용어 | 높음 |
| 3 | 31:05 | 앙상블 ~~학습~~ | 앙상블 학습(?) | 불명확 | ⚠️낮음 |
```

---

## 4. 프로젝트 구조 (구현 시)

```
stt-conference/
├── pyproject.toml
├── config/
│   ├── profiles/
│   │   ├── seminar.yaml      # 학회: diarization off, 용어집 필수, Q&A 템플릿
│   │   ├── lecture.yaml      # 강연: 단일화자, 목차형 요약
│   │   └── meeting.yaml      # 회의: diarization on, 액션아이템
│   └── glossaries/           # 분야별 용어집 (누적 관리)
│       └── my-field.txt
├── src/stt_pipeline/
│   ├── cli.py                # `stt run recording.wav --profile seminar`
│   ├── preprocess.py         # ffmpeg 변환, VAD
│   ├── transcribe.py         # faster-whisper 래퍼
│   ├── diarize.py            # pyannote (회의 프로필)
│   ├── correct.py            # Claude 교정 + diff 검증
│   ├── summarize.py          # 프로필별 요약
│   └── report.py             # md/json/srt 출력
└── tests/
```

**용어집 자산화**: 교정 리포트에서 확정된 수정 쌍(`크리스퍼 캐스나인 → CRISPR-Cas9`)을 용어집에 자동 누적 → 다음 녹음부터 initial_prompt와 교정 프롬프트에 반영. **쓸수록 정확해지는 구조**가 이 시스템의 장기 경쟁력입니다.

---

## 5. 비용·성능 추정 (1시간 세미나 기준)

| 단계 | 시간 | 비용 |
|---|---|---|
| 전처리 + VAD | ~1분 | 무료 |
| STT (faster-whisper, RTX GPU) | 5~10분 | 무료 (전기세) |
| 화자분리 (회의 시) | ~5분 | 무료 |
| LLM 교정 (~15K 토큰 입력, 청크별) | 수분 (Batch: ~수십 분) | 일반 ~$0.3 / Batch ~$0.15 |
| 요약 | ~1분 | ~$0.1 |
| **합계** | **약 15분 내외** | **회당 500원 미만** |

GPU가 없다면 STT만 OpenAI API로 대체 가능 (1시간 ≈ $0.36, whisper-1 기준).

---

## 6. 구현 로드맵

### Phase 1 — MVP (CLI, 학회 프로필)
- [ ] 전처리(ffmpeg + VAD) → faster-whisper 전사 → md/srt 출력
- [ ] Claude 교정 계층 (청크 분할, structured outputs, diff 검증)
- [ ] 교정 내역 표 + 기본 요약
- **완료 기준**: `stt run seminar.wav --profile seminar --glossary bio.txt` 한 줄로 transcript.md 생성

### Phase 2 — 프로필 완성
- [ ] 회의 프로필: pyannote 화자분리 + 액션아이템 추출
- [ ] 강연 프로필 템플릿
- [ ] 용어집 자동 누적 (교정 결과 → glossary 반영)
- [ ] Batch API 전환 (비용 50% 절감)

### Phase 3 — 편의성
- [ ] 간단한 웹 UI (Gradio): 파일 드롭 → 결과 확인, 교정 내역 승인/거부 버튼
- [ ] 저신뢰 구간 오디오 클립 링크 (클릭하면 해당 구간 재생하며 검수)
- [ ] 여러 녹음 검색 (전사 아카이브)

### 하지 않기로 한 것 (명시)
- ❌ Whisper 파인튜닝 — 용어집 + LLM 교정으로 충분. 필요성이 데이터로 입증되기 전까지 보류
- ❌ 실시간 스트리밍 전사 — 녹음 후 배치 처리가 품질·비용 모두 우위. 학회 현장에서 실시간 자막이 필요해지면 별도 검토
- ❌ 용도별 별도 STT 모델 — §1 결론대로 프로필로 통합

---

## 7. 리스크와 대응

| 리스크 | 대응 |
|---|---|
| LLM이 화자의 원래 표현을 과도하게 다듬음 | diff 검증 + "기록 없는 수정 무효" 규칙 + 롤백 |
| 코드스위칭 구간 오인식 | 용어집 주입 + 저신뢰 구간 집중 교정 + ⚠️ 플래그로 사람 검수 |
| 무음/박수 구간에서 Whisper 환각 | VAD 필터 + 반복 문장 휴리스틱 제거 |
| 긴 녹음에서 청크 경계의 문맥 단절 | 청크 간 500토큰 오버랩 + 직전 청크 요약을 다음 청크 프롬프트에 전달 |
| 민감한 미공개 연구 내용 | STT는 로컬 처리(기본값), LLM 교정만 API — 필요 시 전사 텍스트에서 식별 정보 마스킹 옵션 |
```
